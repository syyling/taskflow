export enum AuthLevel {
  OWNER = "소유자",
  ADMIN = "관리자",
  MEMBER = "멤버",
  VIEWER = "조회자",
}

export enum Role {
  FRONTEND = "프론트엔드",
  BACKEND = "백엔드",
  PLANNER = "기획자",
  PM = "PM",
  DESIGNER = "디자이너",
}
